# ScanKey
 应用用单片机嵌入式的强大的按键动作识别模块，全网识别动作最全。




┌──────────────────────────────┐
|         ScanKeyApp           |
└──────────────────────────────┘
┌──────────────────────────────┐
|          ScanKey.c           |
└──────────────────────────────┘
┌──────────────────────────────┐
|        ScanKeyPort.c         |
└──────────────────────────────┘

``` c
静态或动态创建ScanKey按键对象；
初始化ScanKey按键对象；
执行按键扫描程序；==>>将扫描到的按键动作事件加入到按键消息队列中；
主应用中获取按键消息队列中的按键动作事件并对执行相应按键动作事件处理；


```
